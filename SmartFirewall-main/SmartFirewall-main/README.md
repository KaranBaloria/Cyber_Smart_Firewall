# SmartFirewall
A Java-based application-level smart firewall that inspects incoming HTTP requests using IP reputation checks, rate limiting, and regex-based attack detection (SQLi/XSS). The project includes a real-time web dashboard that displays firewall decisions and logs, simulating a basic security monitoring system for APIs and web applications.
